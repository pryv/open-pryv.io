#!/usr/bin/env node

// Observability boot must come before any other require.
require('../../../bin/_observability-boot');

// wrapper for consistency with the other server components, which all have `bin/server`
require('../src/server');
