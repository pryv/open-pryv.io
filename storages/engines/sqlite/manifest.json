{
  "storageTypes": ["baseStorage", "dataStore", "seriesStorage", "auditStorage"],
  "entrypoint": "src/index.ts",
  "requiredInternals": [
    "userLocalDirectory",
    "getEventFiles",
    "createUserAccountStorage",
    "storageLayer",
    "cache"
  ],
  "configuration": {
    "fields": {
      "path":  { "type": "string",  "required": true,  "description": "Base directory for user SQLite databases and index" }
    }
  }
}
